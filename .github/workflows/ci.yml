# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
# -----------------------------------------------------------------------------
# CI Workflow — Travel-Plan-Permission
# -----------------------------------------------------------------------------
# Uses reusable Python CI workflow from stranske/Workflows.
# See: https://github.com/stranske/Workflows/blob/main/.github/workflows/reusable-10-ci-python.yml
# -----------------------------------------------------------------------------

name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  python:
    uses: stranske/Workflows/.github/workflows/reusable-10-ci-python.yml@v1
    with:
      python-versions: '["3.11", "3.12"]'
      coverage: true
      lint: true
      format_check: true
      typecheck: true
      run-mypy: true
      baseline-coverage: '80'
      coverage-alert-drop: '2'
      enable-metrics: false
      enable-history: false
      enable-coverage-delta: false
      enable-soft-gate: false
    secrets: inherit
# trigger CI
