## needs-human

Comment: The "Configure GitHub Actions for testing" task requires changes under
`.github/workflows/` to add a test workflow and enforce required checks. This
agent run is restricted from editing workflow files, so a human with
`agent-high-privilege` access must apply those updates.
