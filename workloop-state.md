## 2026-06-05T08:10:30Z - opener (codex): issue #1152 create_app route extraction

- Repo/issue: `stranske/Travel-Plan-Permission` #1152 (`Split the 799-line create_app god-function in http_service.py`).
- Branch/worktree: `codex/issue-1152-create-app-routes` in `/Users/teacher/.codex/automations/pd-workloop-resume/worktrees/tpp-1152-create-app-routes`, based on fresh `origin/main` (`de35a65`).
- Selection: raw opener cap below 5. High-priority Trend #5343 and LMS #180 remained scoped; Trend #5389/#5440 remained scoped/non-repairable. Approved queue items in Inv-Man-Intake #527 and Trend #5510 were freshly materialized but found stale/duplicate of already-merged work and closed with evidence. TPP #1151 already has merged PR #1160, making #1152 the oldest unlinked implementation candidate outside scoped blockers.
- Implementation: extracted the inline `create_app` route closures into six top-level registration helpers: `register_portal_routes`, `register_review_routes`, `register_manager_routes`, `register_admin_routes`, `register_artifact_routes`, and `register_planner_api_routes`. Route bodies, paths, response models/classes, auth calls, and proposal-store usage were preserved; `create_app` now only builds runtime state/app, seeds demo data, calls the helpers, and returns the app.
- Validation: `python -m pytest tests/python/test_http_service.py -q` -> 108 passed. `python -m ruff check src/travel_plan_permission/http_service.py` -> passed. `git diff --check` -> passed. Structural gate: `create_app_nonblank_lines=25`, `decorators_in_create_app=0`, `total_route_decorators=23`. Live gate: local service on ephemeral port 65362 with bootstrap-token auth, then `PYTHONPATH=src python -m travel_plan_permission.planner_smoke --base-url http://127.0.0.1:65362` -> Planner HTTP smoke passed.
- Deliberate-break gate: temporarily changed the moved `GET /api/planner/policy-snapshot` registration to `/api/planner/policy-snapshot-disabled`; `python -m pytest tests/python/test_http_service.py::test_snapshot_route_returns_planner_contract -q` failed with 404 vs 200. Restored the route and reran the full suite green.
- Current state: ready to commit, push, and open a ready-for-review PR with `agent:codex`, `agents:keepalive`, and `autofix`; post-open action is Gate Followups dispatch if cap-health needs evidence.
